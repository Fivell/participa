<p><%= t('devise.mailer.welcome', user: @resource.first_name) %></p>

<p><%= t('devise.mailer.confirm.p1') %></p>

<p>
  <% url = confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %>
  <%= link_to url, url %>
</p>
