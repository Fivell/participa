<%= t('devise.mailer.welcome', user: @resource.first_name) %>

<%= t('devise.mailer.confirm.p1') %>

<%= link_to t('devise.mailer.confirm.link'), confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %>

<%= t('devise.mailer.confirm.text_only_instructions') %>

<%= confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %>
