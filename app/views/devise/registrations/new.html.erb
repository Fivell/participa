<% content_for :title, t("devise.titles.sign_up") %>

<div class="content-content">
  <div class="subtitle-welcome box-info">
    <%= content_tag(:h3, t("devise.titles.sign_up")) %>
    <%= t('bec.registration.message.welcome_html') %>
  </div>

  <div class="box-info">
    <%= render partial: "form", locals: {action: "new"} %>
  </div>
</div>
