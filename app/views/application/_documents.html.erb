<% upload.documents.find_each do |document| %>
  <li>
    <%= image_tag document.scanned_picture.url(:thumb),
                  data: { original: document.scanned_picture.url(:original) } %>
  </li>
<% end %>
